# issue DAWモード、ランダム音色変更時にmeas1側のWAV再renderが走るはずなのに、その表示がされない #67
[issues #67](https://github.com/cat2151/clap-mml-render-tui/issues/67)

- userがほしいもの：
    - meas1～8は、それぞれ、4/4 BPM120 であれば、2secずつのcache wavを生成し、local confingにファイル保存する
    - meas1～8は、それぞれ、4/4 BPM120 であれば、2secずつのcache audio dataを生成し、メモリに保持する
    - meas0 のランダム音色に変化があった場合は、meas1～8のcache wavファイルとcache audio dataは、ランダム音色にあわせて即座に再生成を開始する。再生成はbackgroundで、可能であれば並列に実施されることで短時間で生成を完了する。

- 状況
    - 実装されたようなので動作確認する
