# issue logの「cargo check: 」について、「repo name」「check対象filenameと、取得結果文字列のセット」「checkコマンドと、取得結果文字列のセット」もlogに出力する #123
[issues #123](https://github.com/cat2151/clap-mml-render-tui/issues/123)

- 要は、「どのrepoに対して」「なにを（どのファイルをread、どのコマンドを実行）行った結果、なにを（どのような具体的な文字列を）取得できたのか？」
- をlogに出力する、ということ
- 現在のlogでは、データ不足で、調査には使えないので。

