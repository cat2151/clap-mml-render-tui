[package]
name = "cmrt-runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "cmrt_runtime"
path = "src/lib.rs"

[dependencies]
cmrt-surge-patches = { path = "../surge-patches" }
anyhow = "1"
dirs = "6"
serde = { version = "1", features = ["derive"] }
toml = "1"
